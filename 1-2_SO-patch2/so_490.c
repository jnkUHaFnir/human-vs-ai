    int copyText(char from[],char to[],int max)
    {
        if (new_strlen(from) < max)
        {
            int i;
            for(int i = 0; from[i] != '\0'; i++)
                to[i] = from[i];
            }
    
            to[i] = '\0';
        }
    
        return 0;
    }