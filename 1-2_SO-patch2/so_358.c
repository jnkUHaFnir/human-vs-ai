    char array[1000];
    scanf("%999s",array);
    size_t len = strlen(array);
    for( size_t i = 0; i < len; i++ ){
       if( arrau[i] == 'A' ){
          ...
       } 
       else if( .. ){
    
       }
    }