void merge(int arr[], int l, int m, int r) 
{ 
    // ... [unchanged code]

    // Print left subarray
    printf("\n");
    for (i = 0; i < n1; i++)
        printf("%d ", L[i]);

    // Print right subarray
    printf("\n");
    for (j = 0; j < n2; j++)
        printf("%d ", R[j]);
    printf("\n");

    // ... [rest of the merge logic without changes]
} 
1 3 5 12 14 

Sorted array is 
1 3 5 12 14
