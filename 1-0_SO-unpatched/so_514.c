    int n;
    int x;
    char sp[3];
    while ((n = scanf("%d%2[ \t\n]", &x, sp)) != 0)
    {
        if (n == 2)
            // got an integer and 2 white space characters after it
        else
            // got an integer and not two white space characters after it
    }