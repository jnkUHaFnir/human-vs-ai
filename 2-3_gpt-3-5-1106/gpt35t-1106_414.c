tmp = getString();
rub[idx].name = (char *) malloc(strlen(tmp) + 1);
strcpy(rub[idx].name, tmp);
// Repeat the above for surname, address, and number.
ttmp[i].name = (char *) malloc(strlen(rub[k].name) + 1);
strcpy(ttmp[i].name, rub[k].name);
// Repeat the above for surname, address, and number.
