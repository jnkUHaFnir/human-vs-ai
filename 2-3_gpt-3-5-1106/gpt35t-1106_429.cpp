if (ground_truth_filename == NULL)
char* ground_truth_filename = NULL;
if (ground_truth_filename == NULL || ground_truth_filename[0] == '\0') {
    // Assign default value or handle the case when no filename is provided
}
