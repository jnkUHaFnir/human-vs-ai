
In this updated code, the `del_p` function takes a double pointer to a node `struct Node** current` instead of a pointer to a node `struct Node* current`. By doing this, we can modify the original head pointer `head` in the `main` function if the last node is deleted.
