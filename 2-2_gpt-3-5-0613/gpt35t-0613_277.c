while (fscanf(file, "%d%s", &number, letter) != EOF) {
    // do something with the number and letter, if needed
}
array = (Foo**)malloc(sizeof(Foo*) * N);
for (int j = N - 1; j >= i; j--) {
    // sorting code
}
