struct futbaler
{
    char ime[15];
    char prezime[20];
    char pozicija[15];
    char godiste[5]; // Allow space for null terminator
};
printf("Ime igraca #%d: ", i + 1);
fgets((pok + i)->ime, sizeof((pok + i)->ime), stdin);
(pok + i)->ime[strcspn((pok + i)->ime, "\n")] = '\0'; // Remove newline character
#include <string.h>
