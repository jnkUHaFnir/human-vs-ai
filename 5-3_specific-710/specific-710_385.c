alive_row = atoi(&line[0]);    // This sets alive_row to the first character of the line
alive_column = atoi(&line[2]); // This sets alive_column to the third character of the line
alive_row = atoi(&line[0]);    // This correctly sets alive_row to the first character of the line
alive_column = atoi(&line[2]); // This correctly sets alive_column to the second character of the line
